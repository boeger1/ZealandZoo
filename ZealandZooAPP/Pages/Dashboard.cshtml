@page
@using ZealandZooLIB.Models;


@model ZealandZooAPP.Pages.DashboardModel
@{
}

@if (User.IsInRole("admin"))
{

    <head>
        <link href="~/css/site.css" rel="stylesheet" />

        <style>
            .calendar-container {
                display: flex;
                flex-wrap: wrap;
                justify-content: flex-start;
                padding-right: 5px;
            }


            .calendar-event-box {
                width: 250px;
                padding: 10px;
                margin: 10px;
                backdrop-filter: blur(5px) saturate(119%);
                -webkit-backdrop-filter: blur(5x) saturate(119%);
                background-color: rgba(245, 115, 10, 0.15);
                border-radius: 20px;
                border: 4px solid rgba(245, 115, 10, 0.35);
                z-index: 1;
                outline: none;
                overflow: hidden;
                transition: color 0.4s ease-in-out;
            }

            .leftDash{
                width:20%;
                float: left;
                margin: 5%
            }

            .mainDash {
                width: 50%;
                float: left;
                margin: 5%;
            }

            .rightDash{
                width: 30%;
                float: left;
                margin: 5%
            }
            
        </style>



    </head>


    <section>
        <div class="leftDash">
          
               
                    <ul>
                        <li class="nav-item">
                            <form method="get">
                                <button asp-page="/StoragePage">Lager</button>
                            </form>
                        </li>

                        <li class="nav-item">
                            <form method="get">
                                <button asp-page="/BulletPage">Opslag</button>
                            </form>
                        </li>

                        <li class="nav-item">
                            <form method="get">
                                <button asp-page="/EventCRUD/EventPage">Event</button>
                            </form>
                        </li>
                    </ul>
               
           </div>

           
              <div class="mainDash">
                    rgijjegheøg
                    gjthkrtjh
                    hrtgtbtr

                    bhgtrhbgfrth
                    ikjl
                
                </div>

            

                
                    <div class="rightDash">
                  
                    <h2>Events</h2>
                        @foreach (Event e in Model.Events)
                        {
                            <div class="calendar-event-box">
                                <div class="calendar-event-title">@e.Name</div>
                                <div class="calendar-event-date">@e.DateFrom.ToString("MMMM dd, yyyy")</div>

                            </div>
                        }
                    </div>
                
           
        
    </section>
        






































}







   
  
